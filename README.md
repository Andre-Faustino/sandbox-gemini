# gemini-sandbox

## Setup
1) Node 20+
2) `cp .env.example .env` e preencha GEMINI_API_KEY
3) `npm i`

## Run
`npm run dev`

Outputs:
- `out/result.json` (o JSON validado)
- `out/generated/` (arquivos materializados do retorno)
